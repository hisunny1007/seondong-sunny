// 2. 1부터 N까지 출력 2

// - 변수 `N` 을 선언하고, 임의의 숫자형 값을 할당한다.
// - 변수 `N` 의 값을 출력한다.
// - 1부터 `N` 까지 출력하는 `while` 반복문을 작성한다.

let N = 14;
console.log(N);

let num = 1;

while (num <= N) {
  console.log(num);
  num++;
}

// 1부터 N까지 출력하는 while 반복문
let i = 1;
while (i <= N) {
  console.log(i);
  i++;
}