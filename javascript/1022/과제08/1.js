// 1. 1부터 N까지 출력 1

// - 변수 `N` 을 선언하고, 임의의 숫자형 값을 할당한다.
// - 변수 `N` 의 값을 출력한다.
// - 1부터 `N` 까지 출력하는 `for` 반복문을 작성한다.
// - 실행 결과

let N = 13;
console.log(N);

// 1부터 N까지 출력하는 for 반복문
for (let i = 1; i <= N; i++) {
  console.log(i);
}

