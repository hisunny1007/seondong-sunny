// 함수 2번. 바깥에서 안은 불가능

const { intersection } = require("lodash")

// 아래에서 위 불가능


// parse intersection

// include split join trim

function fuc (a,b) {
  let sum = a + b
   return sum
}
 

let result = fuc(1,2)
console.log(result)